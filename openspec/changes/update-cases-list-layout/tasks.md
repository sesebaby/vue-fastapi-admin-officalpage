## 1. Cases list UI adjustments
- [ ] Drop the amount column/layout helpers and restyle the card as a single-row flex container.
- [ ] Keep typography responsive so titles and metadata scale cleanly on mobile/desktop.

## 2. Data & locale updates
- [ ] Extend the cases dataset with the 2025-10-18 magnetic stirring system entry and remove unused amount keys.
- [ ] Localize the new title/project strings in cn/en JSON files.

## 3. Validation
- [ ] Smoke-test the /cases route in both languages to confirm the inline layout and new entry render without console errors.
